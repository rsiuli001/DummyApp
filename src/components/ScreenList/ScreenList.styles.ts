import { StyleSheet } from 'react-native';

const ScreenListStyles = StyleSheet.create({
  container: {
    marginLeft: 20,
  },
  navigationText: {
    fontWeight: '400',
    fontSize: 18,
  },
});

export default ScreenListStyles;
