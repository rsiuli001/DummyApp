import { StyleSheet } from 'react-native';

const ScreenThreeStyles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    marginTop: 20,
  },
  textContainer: {
    flexDirection: 'row',
    justifyContent: 'flex-end',
    alignItems: 'center',
    marginRight: 20,
    marginBottom: 10,
  },
  text: {
    fontWeight: '400',
    fontSize: 16,
  },
});

export default ScreenThreeStyles;
